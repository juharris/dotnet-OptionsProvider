metadata:
    # This is an example that is also used in tests.
    isPersistent: true
    owners: Justin@site.com
options:
    config:
        myConfigurableString:
            template: "{{2}}{{1}}{{more}}"
            values:
                more: " How are you?"
